/** Keep in sync with the inline script in `google-consent-bootstrap.tsx`. */
export const COOKIE_CONSENT_STORAGE_KEY = "mh_cookie_consent";

export type CookieConsentChoice = "accepted" | "rejected";
